  
name: Build &Deploy
on:
  push:
    branches: [rajeshsubbranch]
    paths-ignore:
      - 'test.txt'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy
        uses: actions/checkout@v2
        with:
          path: ~/samplerepos

   